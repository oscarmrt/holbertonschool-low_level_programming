#include "holberton.h"
/**
 * mul - multiplies two integers and returns the result
 * @h: Integer
 * @s: Integer
 * Description: write a function that multipliess two integers
 *
 * Return: 0
 */
int mul(int h, int s)
{
	return (h * s);
}
