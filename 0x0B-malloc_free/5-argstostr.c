#include "holberton.h"
#include <stdio.h>
#include <stdlib.h>
/**
 * *argstostr - Entry point
 *
 * Description: write a function that concatenates all the arguments
 * of your program
 * @ac: Is the parameter to be checked
 * @av: Is the parameter to be checked
 * Return: Always 0 (Success)
 */
char *argstostr(int ac, char **av)
{
	int k;

	if (ac == 0)
		return (NULL);
	if (av == NULL)
		return (NULL);

}
